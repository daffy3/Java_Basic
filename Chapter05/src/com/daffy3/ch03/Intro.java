package com.daffy3.ch03;

public class Intro {
    // 02. 연결리스트(Linked-List) 만들기

    // 연결리스트(Linked-List) 특징
    // - 동일한 데이터 타입을 순서에 따라 관리하는 구조
    // - 자료를 저장하는 노드에는 자료와 다음 요소를 가리키는 링크(포인터)가 있음
    // - 자료가 추가될 때, 노드 만큼의 메모리를 할당 받고, 이전 노드의 링크로 연결한다.(정해진 크기는 없음)
    // - 연결 리스트의 i번째 요소를 찾는게 걸리는 시간은 요소의 개수에 비례
    // - JDK 클래스: LinkedList
}
